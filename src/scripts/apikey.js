/* 

If I had an API key I'd put it here

for example...
const nashvilleOpenDataApiKey = "__SOME_KEY__";

This file would be referenced in my index.html too.
AND it would be in my .gitignore. Putting it in the .gitignore means that each
team member will need to make their own instance of this file. Each team member
may use the same key...but don't share with other teams.

*/
